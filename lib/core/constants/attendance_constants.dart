/// Constants for the attendance tracking feature.
class AttendanceConstants {
  AttendanceConstants._();

  /// Minimum attendance percentage before showing warning (below this = at risk).
  static const double attendanceThreshold = 75.0;
}
